/** Versión de la app — incrementar en cada release (v1.2, v1.3, …). */
export const APP_VERSION = 'v1.3';

export const APP_VERSION_LABEL = APP_VERSION;

/** Notas breves del release actual (visible en sidebar). */
export const APP_VERSION_NOTES = 'Pestañas de fase en posiciones, filtros y optimización de grupos';
