/** Fase 1: fase regular (grupos / round robin). */
export const PHASE_REGULAR = 1;

/** Fase 2: segunda rueda (grupos + liga, arrastre de puntos desde fase 1). */
export const PHASE_SECOND_ROUND = 2;

/** Fase 3: playoffs (eliminación directa / cuadro). */
export const PHASE_PLAYOFF = 3;
